<!DOCTYPE html>
<html>
  <head>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <title>LUNCHOICE</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= display_meta_tags(default_meta_tags) %>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <link href="https://fonts.googleapis.com/css2?family=Courgette&family=Shantell+Sans:ital,wght@0,300..800;1,300..800&display=swap" rel="stylesheet">
  </head>

  <body>
    <header class="navbar navbar-expand-lg">
      <div class="container-fluid px-4">
        <a class="navbar-brand" href="/">lunchoice</a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
          <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
          <ul class="navbar-nav gap-2">
            <li class="nav-item">
              <%= link_to "みんなのランチ", posts_path, class: "btn btn-custom w-100" %>
            </li>
            <% if user_signed_in? %>
              <li class="nav-item"><%= link_to "マイページ", user_path(current_user), class: "btn btn-custom w-100" %></li>
              <li class="nav-item"><%= link_to "ランチを投稿", new_post_path, class: "btn btn-custom w-100" %></li>
              <li class="nav-item"><%= link_to "食事履歴", choices_path, class: "btn btn-custom w-100" %></li>
              <li class="nav-item"><%= link_to "ログアウト", destroy_user_session_path, data: { turbo_method: :delete }, class: "btn btn-custom w-100" %></li>
            <% else %>
              <li class="nav-item"><%= link_to "ログイン", new_user_session_path, class: "btn btn-custom w-100" %></li>
            <% end %>
          </ul>
        </div>
      </div>
    </header>

    <% if notice %> <div class="notice"><%= notice %></div> <% end %>
    <% if alert %> <div class="alert"><%= alert %></div> <% end %>

    <main>
      <%= yield %>
    </main>
    
    <footer>
      <div class="container">
        <div class="mb-3">
          <%= link_to "使い方", guide_path, class: "footer-link" %>
          <%= link_to "利用規約", terms_path, class: "footer-link" %>
          <%= link_to "プライバシーポリシー", privacy_path, class: "footer-link" %>
        </div>
        <p class="mb-0 small">&copy; <%= Time.current.year %> LUNCHOICE. All rights reserved.</p>
      </div>
    </footer>
  </body>
</html>